# IT Asset Dashboard (React + Vite)

ระบบ Dashboard แสดงผลครุภัณฑ์คอมพิวเตอร์แบบ Modern Web Application ที่พัฒนาด้วย React, Vite และ Tailwind CSS โดยเน้นประสิทธิภาพ ความสวยงาม และความปลอดภัย

## 🚀 ฟีเจอร์หลัก (Key Features)

### 1. การแสดงผลและกรองข้อมูล (Data & Filtering)
-   **CSV Integration**: ดึงข้อมูลสดจาก Google Sheets CSV (ตั้งค่าผ่าน Environment Variable)
-   **Smart Filters**: กรองข้อมูลได้ละเอียด (Department, Type, OS, CPU, RAM, Disk, etc.)
-   **Search**: ค้นหาแบบ Real-time ตามชื่อคน, รหัสเครื่อง หรือชื่อเครื่อง

### 2. ระบบประเมินสุขภาพเครื่อง (Health Grading System) ✨
ระบบจะคำนวณคะแนน (0-100) และตัดเกรด (A-D) โดยอัตโนมัติตามเกณฑ์:
-   **RAM (Max 30)**: ≥16GB=30, ≥8GB=20, <8GB=5
-   **Disk Type (Max 40)**: NVMe/M.2=40, SSD=30, HDD=10
-   **OS (Max 30)**: Win11=30, Win10=20, Old=0
-   **Penalty**: หัก 20 คะแนน หากใช้งาน Harddisk เกิน **43,800 ชั่วโมง**

### 3. หน้ารายละเอียด (Asset Detail Modal) ✨
-   แสดงข้อมูล Hardware เชิงลึก (Disk 1, Disk 2)
-   **ใหม่! Score Breakdown**: แสดงตารางแจงคะแนนย่อย ว่าได้คะแนนจากส่วนไหนบ้าง (RAM, Disk, OS) และโดนหักคะแนนหรือไม่
-   **Visual Warning**: ตัวหนังสือสีแดงแจ้งเตือนเมื่อ Disk ใช้งานหนักเกินเกณฑ์

### 4. กราฟและสถิติ (Charts & Stats)
-   **Platform Distribution**: กราฟวงกลมแยกสัดส่วน Windows 11 vs 10
-   **Grade Summary**: กราฟแท่งแสดงจำนวนเครื่องในแต่ละเกรด (A, B, C, D)

---

## 🛠 วิธีการติดตั้งและรันโปรเจกต์ (Installation & Run)

### 1. เตรียมเครื่อง (Prerequisites)
ต้องติดตั้ง [Node.js](https://nodejs.org/) ก่อน

### 2. ติดตั้ง (Install Dependencies)
```bash
npm install
```

### 3. รันทดสอบ (Development Mode)
```bash
npm run dev
```
เปิด Browser ไปที่ `http://localhost:5173`

### 4. สร้างไฟล์สำหรับใช้งานจริง (Production Build)
```bash
npm run build
```
จะได้โฟลเดอร์ `dist/` สำหรับนำไปวางบน Web Server

---

## ☁️ การ Deploy ขึ้น Vercel

### ขั้นตอนที่ 1: เตรียมโค้ด
ใช้ Git ในการส่งโค้ดขึ้น Server:
```bash
git add .
git commit -m "Update message..."
git push
```

### ขั้นตอนที่ 2: ตั้งค่า Vercel (ครั้งแรก)
สำคัญมาก! ต้องตั้งค่า **Environment Variables** ในหน้า Settings ของ Project บน Vercel:
*   **Key**: `VITE_GOOGLE_SHEET_CSV_URL`
*   **Value**: (URL ของ Google Sheet CSV)

*หากมีการอัปเดต URL หรือแก้โค้ด อย่าลืมกด **Redeploy** ในหน้า Deployments*

---

## 📁 โครงสร้างไฟล์ (Project Structure)
```
it-asset-dashboard/
├── .env                   # เก็บ Config ลับ (Git ignore)
├── src/
│   ├── App.jsx            # หน้า Dashboard หลัก
│   ├── main.jsx           # Entry Point
│   ├── components/        # ส่วนประกอบย่อย
│   │   ├── AssetDetailModal.jsx  # หน้าต่างรายละเอียด + Score Breakdown
│   │   ├── FilterSection.jsx     # เมนูตัวกรอง
│   │   └── StatCard.jsx          # การ์ดสถิติ
│   └── utils/
│       └── dataHelpers.js # Logic คำนวณคะแนน (Grading Algo)
└── README.md              # คู่มือการใช้งานไฟล์นี้
```
